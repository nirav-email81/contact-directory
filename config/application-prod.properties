# Production environment — user profile directory, isolated from dev data
environment=production
# database.file is optional; default is %LOCALAPPDATA%\ContactDirectory\contacts.db on Windows
legacy.file=contacts.txt
window.title=Personal Contact Directory
logging.level=WARNING
